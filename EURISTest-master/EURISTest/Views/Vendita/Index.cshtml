@model IEnumerable<EURISTest.Models.Vendita>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            Catalogo
        </th>
        <td></td>
        <th>
            Prodotto
        </th>
        <td></td>
        <th>
            @Html.DisplayNameFor(model => model.Prezzo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cataloghi.Nome)
        </td>
        <td></td>
        <td>
            @Html.DisplayFor(modelItem => item.Prodotti.Nome)
        </td>
        <td></td>
        <td>
            @Html.DisplayFor(modelItem => item.Prezzo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.VenditeID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.VenditeID })
        </td>
    </tr>
}

</table>
